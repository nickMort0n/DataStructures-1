#include <stdio.h>
#include "main.h"


void initQueue() {
    struct node* head;
    struct node* tail;
    head = tail = NULL;
}

int isEmpty(struct queue* head) {
    return (head == NULL);//when head is NULL the list is empty
}

